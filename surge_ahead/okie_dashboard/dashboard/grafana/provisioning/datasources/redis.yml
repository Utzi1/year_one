apiVersion: 1

datasources:
  - name: Redis
    type: redis-datasource
    uid: glZcolIVz
    access: proxy
    url: redis:6379
    database: 0
    jsonData:
      client: standalone
      poolSize: 5
      timeout: 10
      pingInterval: 0
      pipelineWindow: 0
    # secureJsonData:
    #   password: your_redis_password
    isDefault: true